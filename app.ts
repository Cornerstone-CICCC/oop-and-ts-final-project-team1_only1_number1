// import { setupSearch } from "./components/Search";
// import { openModal, closeModal } from "./components/Modal";
// import { addTask, deleteTask, tasks } from "./components/TaskManager";

// setupSearch(tasks);

// document.getElementById("addTaskBtn")?.addEventListener("click", () => {
//   addTask("add");
// });

// document.getElementById("openModalBtn")?.addEventListener("click", () => openModal("taskModal"));
// document.getElementById("closeModalBtn")?.addEventListener("click", () => closeModal("taskModal"));